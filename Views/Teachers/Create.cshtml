@model Models.Teacher

@{
    ViewBag.PageTitle = "Prof - Ajout";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <details open>
        <summary>Informations</summary>

        <label>Prénom</label>
        @Html.TextBoxFor(m => m.FirstName, new { @class = "form-control" })

        <label>Nom</label>
        @Html.TextBoxFor(m => m.LastName, new { @class = "form-control" })

        <label>Date d'embauche</label>
        @Html.TextBoxFor(m => m.StartDate, "{0:yyyy-MM-dd}", new { @class = "form-control datepicker" })

        <label>Téléphone</label>
        @Html.TextBoxFor(m => m.Phone, new { @class = "form-control phoneExt" })

        <label>Avatar</label>
        @Html.TextBoxFor(m => m.Avatar, new { @class = "form-control" })
    </details>
}